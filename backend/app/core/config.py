"""Configuration loading (placeholder).

Reads environment variables (names listed in .env.example) into typed settings,
grouped by module prefix (EXTRACTION_*, MATCHING_*, RULES_*, ASSISTANT_*,
REPORT_*) plus the shared SUPABASE_* and BACKEND_* settings. No values are
hardcoded here.
"""
