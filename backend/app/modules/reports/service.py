"""reports module — public interface (placeholder).

The ONLY file other modules may import from modules/reports/.
Deterministic templating over human-approved data; no AI calls.
Accepts and returns app/shared/ schema objects exclusively.
"""
