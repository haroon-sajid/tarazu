"""Public interface of the reports module (placeholder).

This is the only file other modules may import from modules/reports/.
Deterministic templating over human-approved data; no AI calls. It accepts and
returns app/shared/ schema objects exclusively.
"""
