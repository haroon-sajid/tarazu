[pytest]
# `backend` on the import path so tests import the app as `app.*`, matching
# how uvicorn loads it (`uvicorn app.main:app --app-dir backend`).
pythonpath = backend
testpaths = backend/tests
addopts = -q
